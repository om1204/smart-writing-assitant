# LexiFlow React Frontend

Premium React/Vite interface for the Smart Reading & Writing Assistant. The frontend calls the FastAPI api_server at `VITE_API_URL` (default `http://localhost:8000`).

## Run
```bash
npm install
npm run dev
```
